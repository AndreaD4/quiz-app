- content_for :page_title, t('devise.mailer.confirmation_instructions.subject')

%p= t('.welcome_html', full_name: @resource.full_name)
%p= t('.click_to_confirm')
%p= link_to t('.confirm_button'), confirmation_url(@resource, confirmation_token: @token, locale: I18n.locale, host: @resource.company.host), class: 'btn btn-primary'
%p= t('.else_ignore')
%p
  %br
  %br
  –––––––––––––––––––––––––––
  %br
  Questo messaggio è stato generato automaticamente, si prega di non rispondere a questa mail.