<div class="card">
  <div class="row align-items-center">
    <div class="col-md-12">
      <div class="card-body">
        <div class="text-center">
          <img alt="logo" src="/logo-erica.png" class="img-fluid mb-4" style="max-height: 200px">
          <h4 class="mb-4 f-w-400">Cambio Password</h4>
        </div>
        <p class="text-dark mb-4">Scegli una nuova password che rispetti i seguenti criteri di sicurezza. <br> La
          password deve contenere almeno 8 caratteri, una lettera minuscola, una lettera maiuscola ed un
          numero.<br>La nuova password deve essere diversa dalle ultime 5 e da quelle utilizzate negli
          ultimi 60 giorni.</p>
        <%= form_for(resource, as: resource_name, url: password_path(resource_name), method: :put, :html => {class: 'needs-validation', novalidate: ""}) do |f| %>
          <%= f.hidden_field :reset_password_token %>
          <div class="form-group mb-2">
            <%= f.password_field :password, autofocus: true, autocomplete: "new-password", class: "form-control", placeholder: "Password", required: true %>
          </div>
          <div class="form-group mb-4">
            <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "form-control", placeholder: "Conferma Password", required: true %>
          </div>
          <%= f.submit "Cambia", data: { turbo: false }, class: "btn btn-block btn-primary mb-4" %>
          <p class="mb-0 text-muted"><%= render "devise/shared/links" %></p>
        <% end %>
      </div>
    </div>
  </div>
</div>

